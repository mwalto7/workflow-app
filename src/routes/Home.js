import React from 'react';
import { Container } from 'semantic-ui-react';
import PricingPage from './PricingPage';

const Home = () => (
  <div>
    <Container>
      <PricingPage />
    </Container>
  </div>
);

export default Home;
